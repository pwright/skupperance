# `skupper system start` Command Reference

```
Start down the Skupper components for the current site

Usage:
  skupper system start [flags]

Examples:
skupper system start -n my-namespace

Flags:
  -h, --help   help for start
```
