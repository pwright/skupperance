# `skupper debug` Command Reference (podman)

```
Usage:
  skupper debug [flags]

Flags:
  -h, --help   help for debug

Global Flags:
  -n, --namespace string   Set the namespace
  -p, --platform string    Set the platform type to use [kubernetes, podman, docker, linux]
```
